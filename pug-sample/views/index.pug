extends layout

block content
  h1= title
  p Welcome to! #{title}
  - var a = title;
  - console.log(`a =>`.rainbow, a);
  - console.error(`title =>`.rainbow, title);
  p After Computed Inline Script!!
  pre #{1 + 2 + ` - ${a}`}
  - var tmpValue = 100;
  pre #{tmpValue} [note! inline variable not in locals scope.]
  pre #{JSON.stringify(locals, null, 2)}
